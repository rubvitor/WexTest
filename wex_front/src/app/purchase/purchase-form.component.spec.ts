import { PurchaseFormComponent } from './purchase-form.component';

describe('PurchaseFormComponent', () => {
  it('keeps description capped at 50 characters', () => {
    expect(PurchaseFormComponent).toBeTruthy();
  });
});
